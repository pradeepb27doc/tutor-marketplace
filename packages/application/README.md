# Application Package

Use cases, ports, command/query contracts, and application orchestration shared by API and worker apps.

This package defines what the system does without binding to HTTP, Prisma, Redis, or provider SDKs.

