# Infrastructure Package

Adapters for Prisma, Redis, BullMQ, object storage, payment gateways, analytics, notifications, monitoring, and other external systems.

Adapters should implement application ports rather than leaking provider SDKs into domain or application code.

