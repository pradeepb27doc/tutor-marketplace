# Worker App

NestJS background worker for outbox dispatch, notifications, booking expiry, payment reconciliation, reminders, reports, analytics, and AI jobs.

Workers must be idempotent and safe to retry.

