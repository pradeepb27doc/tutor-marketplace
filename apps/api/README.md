# API App

NestJS REST API for parent, tutor, student, admin, booking, payment, and learning operations.

This app owns HTTP controllers only. Business rules belong in domain and application packages, while Prisma, Redis, storage, payment, analytics, and notification adapters belong in infrastructure packages.

